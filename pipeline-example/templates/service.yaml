apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipeline-example.fullname" . }}
  labels:
    {{- include "pipeline-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipeline-example.selectorLabels" . | nindent 4 }}
